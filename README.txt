các câu lệnh để chạy dự án:
	cd backend
	npm install
	sudo mongod --dbpath ~/data/db
	node app.js

	cd frontend
	npm install
	npm run serve

truy cập đường link http://localhost:8080/ để kiểm tra